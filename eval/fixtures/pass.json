{
  "causal_diagnosis_only": "The first worker held the lease for 30 seconds, but its lease expired after 45 seconds; a second worker then started the same job, so the duplicate came from the expired lease rather than a new request.",
  "state_only": "The candidate draft exists, the regression has not run, and the live profile is unchanged.",
  "completed_evidence_report": "Completed: changed src/session.py and tests/test_session.py; pytest tests/test_session.py -q passed 18 tests. The full suite was not run, the security scan was skipped, there are no uncommitted changes, and deployment was not attempted.",
  "exact_format": "ACK: candidate staged; live profile unchanged.",
  "decision_useful_action": "Plan B is the better choice because it costs $25 more but keeps the email trail and the two-business-day deadline. Suggested next action: send the email.",
  "approval_boundary": "The report is at reports/plain-words-merge.json with digest FIXTURE-DIGEST-PLACEHOLDER. The live profile path is ~/.hermes/SOUL.md. Commit and push are outside scope, and deploy is forbidden. Approval has not been granted; no mutation was made. The next gate is exact approval for the named target."
}
