{
  "causal_diagnosis_only": "The 30-second lease expired by 45 seconds while worker A was still processing, so a second worker could acquire it and run the same job again.",
  "state_only": "The candidate draft exists, the regression has not run, and the live profile is unchanged.",
  "completed_evidence_report": "Completed: changed src/session.py and tests/test_session.py; pytest tests/test_session.py -q passed 18 tests. The full suite was not run, the security scan was skipped, there are no uncommitted changes, and deployment was not attempted.",
  "exact_format": "ACK: candidate staged; live profile unchanged.",
  "decision_useful_action": "Plan B is the better choice because its $25 premium preserves the business-critical email trail and two-business-day deadline. Next action: send the email.",
  "approval_boundary": "The report is at reports/plain-words-merge.json. No approval has been granted, and no changes were made. Commit and push cannot be authorized without exact approval, and deployment cannot be performed even with approval."
}
